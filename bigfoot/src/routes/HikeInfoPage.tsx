import { Box,} from '@mui/material';
import { green } from '@mui/material/colors';

const HikeInfoPage = () => {

  return (
    <div>
        <Box sx={{ display: 'flex', justifyContent: 'center', marginTop: 2, color: green[500]}}>
                Hike Info
        </Box>
    </div>
  );
};

export default HikeInfoPage;
